<% content_for(:title, "Mario's Specialty Food Products") %>

<h1>All Products</h1>

<ul>
  <% @products.each do |product| %>
    <li><%= link_to product.name, product_path(product) %></li>
  <% end %>
</ul>

<hr>

<%= link_to "New Product", new_product_path %>
